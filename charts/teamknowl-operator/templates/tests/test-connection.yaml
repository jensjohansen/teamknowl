apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teamknowl-operator.fullname" . }}-test-connection"
  labels:
    {{- include "teamknowl-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teamknowl-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
