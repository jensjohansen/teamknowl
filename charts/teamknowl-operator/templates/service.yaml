apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamknowl-operator.fullname" . }}-metrics
  labels:
    {{- include "teamknowl-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: https
  selector:
    {{- include "teamknowl-operator.selectorLabels" . | nindent 4 }}
